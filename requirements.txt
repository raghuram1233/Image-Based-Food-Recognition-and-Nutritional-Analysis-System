# Dependencies are declared in pyproject.toml. This file is kept for
# convenience / tooling that expects a requirements.txt.
#
# Recommended installs:
#   pip install -e .            # measurement tool only (light)
#   pip install -e ".[ml]"      # + fruit classification / training (TensorFlow)
#   pip install -e ".[ml,dev]"  # + ruff / pytest for development

# --- base (measurement) ---
numpy>=1.23
opencv-python>=4.5

# --- ml extra (classification + training) ---
tensorflow>=2.12
pillow>=9.0
matplotlib>=3.5
